void fib(int x)
{
    int f; int g; int i;
    f = 0; g = 1; i = 0;
    while (i < x) {
        f = f + g;
        g = f - g;
        output(g);
        i = i + 1;
    }
}

int main()
{
    fib(input());
    return 0;
}
